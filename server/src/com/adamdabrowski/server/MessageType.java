package com.adamdabrowski.server;

public enum MessageType {
    NYI, CHAT, INFO, BYE, NICKNAME, HELLO, LOGIN,
    LOGGED_IN, LOGGED_OUT, LOBBY_JOIN, LOBBY_LEAVE,
    GO_LEFT, GO_RIGHT, GO_UP, GO_DOWN, ATTACK,
}
